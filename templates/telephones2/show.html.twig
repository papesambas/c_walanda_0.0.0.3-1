{% extends 'base.html.twig' %}

{% block title %}Telephones2{% endblock %}

{% block body %}
    <h1>Telephones2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ telephones2.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ telephones2.numero }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ telephones2.createdAt ? telephones2.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ telephones2.updatedAt ? telephones2.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ telephones2.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_telephones2_index') }}">back to list</a>

    <a href="{{ path('app_telephones2_edit', {'id': telephones2.id}) }}">edit</a>

    {{ include('telephones2/_delete_form.html.twig') }}
{% endblock %}
